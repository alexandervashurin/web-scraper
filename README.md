# Web Scraper

Модульный веб-скрапер на Clojure с поддержкой статических и динамических сайтов, экспортом данных в CSV/JSON и гибридным парсингом через Python.

## 🚀 Возможности

| Режим | Описание | Технологии |
|-------|----------|------------|
| **Static** | Парсинг сайтов без JavaScript | `clj-http`, `hickory` |
| **Dynamic** | Сайты с рендерингом на клиенте | `etaoin` (ChromeDriver) |
| **Python Bridge** | Гибридный парсинг через Python | `requests`, `BeautifulSoup` |

## 📦 Установка

### Требования

- **Java 11+**
- **Clojure 1.11+** (или Leiningen)
- **Python 3.8+** (для Python Bridge)
- **ChromeDriver** (для динамического парсинга)

### Зависимости Python

```bash
pip install requests beautifulsoup4
```

### Сборка проекта

```bash
# Установка зависимостей
lein deps

# Сборка Uberjar
lein uberjar
```

## 🔧 Использование

### Запуск через Leiningen

```bash
# Запуск с тестовыми URL
lein run

# Запуск с загрузкой целей из файла
lein run --config
```

### Запуск из JAR

```bash
java -jar target/uberjar/web-scraper-0.1.0-SNAPSHOT.jar
```

### Настройка целей

Создайте файл `resources/targets.edn`:

```clojure
[{:url "https://example.com" :type :static}
 {:url "https://example.com" :type :dynamic}
 {:url "https://example.com" :type :python}]
```

## 📁 Структура проекта

```
web-scraper/
├── src/web_scraper/
│   ├── core.clj          # Точка входа, оркестрация
│   ├── parser.clj        # Static и Dynamic парсеры
│   ├── python_bridge.clj # Интеграция с Python
│   ├── export.clj        # Экспорт в CSV/JSON
│   └── config.clj        # Загрузка конфигурации
├── resources/
│   └── scraper.py        # Python-скрипт для гибридного парсинга
├── project.clj           # Конфигурация Leiningen
└── README.md
```

## 🏗 Архитектура

```
┌─────────────────────────────────────────────────────────┐
│                     web-scraper.core                     │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────┐ │
│  │   parser    │  │python_bridge│  │     export      │ │
│  │  (static/   │  │   (hybrid   │  │   (csv/json)    │ │
│  │   dynamic)  │  │   python)   │  │                 │ │
│  └─────────────┘  └─────────────┘  └─────────────────┘ │
└─────────────────────────────────────────────────────────┘
```

## 📊 Формат выходных данных

### JSON

```json
[
  {
    "source": "https://example.com",
    "type": "static",
    "title": "Example Domain",
    "content": "Lorem ipsum..."
  }
]
```

### CSV

```csv
source,type,title,content
https://example.com,static,Example Domain,"Lorem ipsum..."
```

## 🔌 Расширение

### Добавление нового парсера

1. Создайте функцию в `parser.clj`:

```clojure
(defn fetch-custom [url]
  "Парсинг с кастомной логикой"
  (try
    ;; Ваша логика
    {:source url
     :type "custom"
     :title "..."
     :content "..."})
  (catch Exception e
    (println (str "Ошибка: " (.getMessage e)))
    nil)))
```

2. Добавьте обработчик в `core.clj`:

```clojure
(defn process-target [{:keys [url type]}]
  (let [clean-url (str/trim url)]
    (case type
      :static (parser/fetch-static clean-url)
      :dynamic (parser/fetch-dynamic clean-url)
      :custom (parser/fetch-custom clean-url)  ; ← новый тип
      (println "Неизвестный тип"))))
```

## ⚠️ Ограничения

- **Динамический парсинг** требует установленного ChromeDriver
- **Python Bridge** требует Python 3.8+ и установленные зависимости
- Некоторые сайты могут блокировать автоматизированные запросы

## 📝 Лицензия

MIT License

## 🤝 Вклад

1. Fork репозиторий
2. Создайте ветку (`git checkout -b feature/amazing-feature`)
3. Commit изменения (`git commit -m 'Add amazing feature'`)
4. Push (`git push origin feature/amazing-feature`)
5. Откройте Pull Request

---

**Автор:** alexandervashurin@yandex.ru
**Версия:** 0.1.0-SNAPSHOT
